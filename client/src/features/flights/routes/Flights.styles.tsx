import styled from '@emotion/styled';

export const FlightsContainer = styled.main`
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
  max-height: calc(100% - 4rem);
  padding-bottom: 1.5rem;
`;
